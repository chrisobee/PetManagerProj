@model PetManager.Models.ToDoTask

@{
    ViewBag.Title = "Details";
}

<h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Task Name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TaskName)
        </dd>
        <dt class="col-sm-2">
            Special Instructions
        </dt>
        @if (Model.SpecialInstructions == null)
        {
            <dd class="col-sm-10">
                N/A
            </dd>
        }
        else
        {
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.SpecialInstructions)
            </dd>
        }

        <dt class="col-sm-2">
            Pet
        </dt>
        <dd class="col-sm-10">
            @ViewBag.Pet
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.TaskId">Edit</a> |
    <a asp-action="Delete" asp-route-id="@Model.TaskId">Remove Task</a> |
    <a asp-controller="PetOwners" asp-action="Index">Back to Home</a>
</div>
